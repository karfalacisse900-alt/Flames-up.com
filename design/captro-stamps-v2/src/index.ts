export { renderStamp, getTemplate, stampAccessibleLabel, DEFAULT_VARIANT, STAMP_VIEWBOX } from './render-stamp';
export { TEMPLATES } from './templates';
export { EXAMPLES } from './examples';
export type * from './types';
// Import the native/web wrapper explicitly to avoid pulling native dependencies into web.
